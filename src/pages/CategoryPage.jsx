import ItemListContainer from "../components/ItemListContainer/ItemListContainer";

// Página de productos filtrados por categoría
const CategoryPage = () => {
  return (
    <div>
      <ItemListContainer saludo="Categoría:" />
    </div>
  );
};

export default CategoryPage;
